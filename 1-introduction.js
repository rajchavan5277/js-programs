/*
 What is JS ?
 1. JavaScript was initially created to “make web pages alive”.
 2. Javascript is scripting language.   
 3. Javascript is platform independent.
# what js cant do ?
- Is does not provide low level access to memory or CPU. But JS engine can access.
- Add new HTML to the page, change the existing content, modify styles.
- React to user actions, run on mouse clicks, pointer movements, key presses.
- Send requests over the network to remote servers, download and upload files (so-called AJAX and COMET technologies).
- Get and set cookies, ask questions to the visitor, show messages.
- Remember the data on the client-side (“local storage”).

# What CAN’T in-browser JavaScript do?
- It has no direct access to OS system functions
- There are ways to interact with camera/microphone and other devices, But with User permission only
- Same Origin Policy- In browser two windows/ tab are from different domain , port they can't communicate with each other.
- Such limitation does not occur if it is in server side.

KeyWords : Scripts, Engines , Environment  : Can be run(client , server side etc) , history, 

*/
